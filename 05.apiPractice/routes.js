// Global

const HOME = '/';
const SEARCH = '/search';

// Movies

const MOVIES = '/movies';
const MOVIE_DETAIL = '/:id';

const routes = {
  home: HOME,
  search: SEARCH,
  movies: MOVIES,
  movieDetail: MOVIE_DETAIL,
};

export default routes;
